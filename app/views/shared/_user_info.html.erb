<#%= link_to  (current_user, size: 50), current_user %>
<h1><%= current_user.username %></h1>
<span><%= link_to  "プロフィール", current_user %></span>
<span><%= pluralize(current_user.posts.count, "投稿") %></span>